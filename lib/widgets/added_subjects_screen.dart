import 'package:flutter/material.dart';

class AddedSubjectsScreen extends StatelessWidget {
  const AddedSubjectsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
